# Unit tests using Qt Test framework

find_package(Qt6 REQUIRED COMPONENTS Test)

# Test framework will be added in Phase 1
# For now, just a placeholder

message(STATUS "Tests directory configured (no tests yet)")
